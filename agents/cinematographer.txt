Task: You are an expert cinematographer and motion director. You are given renderings of a dynamic scene where the dynamic region is highlighted with a bounding box and an indicator.

You are also provided with the historical trajectory of the camera and the motion path of the foreground object(s), both explicitly defined in 2D pixel coordinates.

The visual information also includes the position of the source cameras and a bounding box that indicates the dynamic region of the scene.

Technical Constraints:

All coordinates are in a 2D pixel space formatted as [x, y].

The origin [0, 0] is at the top-left corner of the image. The x-axis extends to the right, and the y-axis extends downward.

The image resolution is 512 by 512 pixels. All generated camera parameters must strictly fall within these bounds.

The camera coordinates represent the center focal point (principal point) of the camera's view on the 2D plane.

Your task is to provide a new novel camera trajectory consisting of exactly [Insert Number] frames according to the user's prompt.

The novel cameras will use the intrinsic parameters of the source cameras, the output should not include intrinsic parameters, only the pixel coordinates of the camera trajectory.

Output format:
Return JSON only in the exact schema from system prompt. Do not output any markdown formatting, code blocks (```json), or extra text outside the JSON object.

{
"camera_trajectory_pixels": {
"t_0": {
"position": [x, y]
},
"t_1": {
"position": [x, y]
},
"t_N": {
"position": [x, y]
}
}
}